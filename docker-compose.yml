version: '3.8'

services:
  file-server:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - PORT=3000
      - AUTH_USER=${AUTH_USER:-admin}
      - AUTH_PASS=${AUTH_PASS:-changeme}
    volumes:
      - ./files:/app/files
    restart: unless-stopped
